import HomeContent from "@/components/home/HomeContent";

/* Variant 2 — the site as designed: magnetic buttons and full motion. */

export const metadata = {
  title: "Maria Maria — Variante 2 (magnetische Buttons)",
};

export default function Index2Page() {
  return <HomeContent />;
}
